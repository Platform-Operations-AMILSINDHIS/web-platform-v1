import type { NextPage } from "next";
import Layout from "~/components/layout";

const Home: NextPage = () => {
  return (
    <Layout title="Home">
      <div>hi, this is the blog page</div>
    </Layout>
  );
};

export default Home;
