import { Box, Flex } from "@chakra-ui/react";

const Footer = () => {
  return (
    <Box w="full" as="footer">
      <Flex>
        <Flex></Flex>
      </Flex>
    </Box>
  );
};

export default Footer;
