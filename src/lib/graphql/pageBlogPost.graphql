query pageBlogPost($id: String!) {
  blogContentType(id: $id) {
    blogTitle
    author
    dateOfBlog
    blogTags
    blogType
    blogDisplayPicture {
      url
    }
    blogContent {
      json
    }
  }
}
